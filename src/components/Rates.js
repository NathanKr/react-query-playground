import React from 'react'
import {useQuery} from 'react-query'

const Rates = () =>{
    const queryKey = "latestRate";
    const latestRateHandler =   
    {} = useQuery()
    return <div>Rates</div>
}

export default Rates;